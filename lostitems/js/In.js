var touPic = document.getElementsByClassName("touOic");
var userName = document.getElementsByClassName("userName")[0];
/*ajax({
	method: 'POST',
	url: 'http://120.79.86.1:8080/pagination/pagination.action',
	data: {
		user: "用户信息"
	},
	async: true,
	dataType: 'JSON',
	success:function(data) {
		var data = JSON.parse(data);
		if (data.) {
			for(var i=0; i<touPic.length; i++) {
				touPic[i].src = data.
			}
			for(var i=0; i<userName.length; i++) {
				userName[i].textContent = data.
			}
		}else {
			alert(data.)
		}
	},
	fail:function(data) {
		alert('请检查你的网络');
	}
})*/